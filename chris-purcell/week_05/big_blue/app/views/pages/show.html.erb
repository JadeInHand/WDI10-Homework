<h1><%= @ocean.name %></h1>

<p>This ocean's average depth is around <%= @ocean.depth %>m. With an area of around <%= number_to_human @ocean.area %> km².</p>

<%= image_tag @ocean.image %>


<div>
<%= link_to('Change the Ocean', ocean_edit_path(@ocean.id)) %>
<%= button_to('Dry up the Ocean', ocean_path(@ocean.id), :method =>  'delete') %>
</div>